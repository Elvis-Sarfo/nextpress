import { MediaLibraryPage } from '@/components/media/MediaLibraryPage';

export const metadata = {
  title: 'Media Library',
  description: 'Upload, search, and manage media assets.',
};

export default function MediaPage() {
  return <MediaLibraryPage />;
}
