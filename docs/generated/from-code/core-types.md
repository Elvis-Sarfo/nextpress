# core-types

*Generated from `src/core/core/types.ts`. Run `pnpm docs:generate` to update.*

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

## Exports

### PageId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### PostId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### NewsId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### DocumentId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### MenuId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### MenuItemId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### LinkCollectionId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### LinkId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### CommentId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### PrincipalId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### RoleId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### Locale

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### ContentEntryId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### ContentVersionId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### ContentTypeId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### PageId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### PostId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### NewsId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### DocumentId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### MenuId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### MenuItemId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### LinkCollectionId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### LinkId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### CommentId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### PrincipalId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### RoleId

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### Locale

Core types for the CMS kernel.
Branded IDs prevent mixing up different ID types at compile time.
Result<T,E> provides explicit error handling without exceptions.

### ContentEntryId

- **@deprecated** Use PageId or PostId instead

### ContentVersionId

- **@deprecated** Use dedicated version types

### ContentTypeId

- **@deprecated** Content types are now explicit models

### Result

- **@deprecated** Content types are now explicit models

### Ok

- **@deprecated** Content types are now explicit models

### Err

- **@deprecated** Content types are now explicit models

### isOk

- **@deprecated** Content types are now explicit models

### isErr

- **@deprecated** Content types are now explicit models

### unwrapOr

- **@deprecated** Content types are now explicit models

### mapResult

- **@deprecated** Content types are now explicit models

### DomainErrorCode

- **@deprecated** Content types are now explicit models

### DomainError

- **@deprecated** Content types are now explicit models

### ValidationError

- **@deprecated** Content types are now explicit models